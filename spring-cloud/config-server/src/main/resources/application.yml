spring:
  application:
    name: config-server
  cloud:
    config:
      allowOverride: true
      overrideNone: true    
      enabled: false
      server:
        git:
          timeout: 10
          uri: https://github.com/kdsalvy/Spring-Boot.git
          search-paths:
            - ConfigRepo
            
server:
  port: 9000
